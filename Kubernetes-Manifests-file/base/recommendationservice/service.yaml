apiVersion: v1
kind: Service

metadata:
  name: recommendation-service
  labels:
    app: recommendation-app

spec:
  selector:
    app: recommendation-app

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

  type: ClusterIP