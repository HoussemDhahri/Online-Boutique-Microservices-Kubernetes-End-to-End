apiVersion: v1
kind: Service

metadata:
  name: shipping-service
  labels:
    app: shipping-app

spec:
  selector:
    app: shipping-app

  ports:
    - protocol: TCP
      port: 50051
      targetPort: 50051

  type: ClusterIP