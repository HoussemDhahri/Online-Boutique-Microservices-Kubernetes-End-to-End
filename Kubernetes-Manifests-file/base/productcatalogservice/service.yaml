apiVersion: v1
kind: Service

metadata:
  name: productcatalog-service
  labels:
    app: productcatalog-app

spec:
  selector:
    app: productcatalog-app

  ports:
    - protocol: TCP
      port: 3550
      targetPort: 3550

  type: ClusterIP