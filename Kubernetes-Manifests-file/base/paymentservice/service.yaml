apiVersion: v1
kind: Service

metadata:
  name: payment-service
  labels:
    app: payment-app

spec:
  selector:
    app: payment-app

  ports:
    - protocol: TCP
      port: 50051
      targetPort: 50051

  type: ClusterIP